import {
    canAllDrink,
    canSomeoneDrink,
    cesarCodex,
    characterFilter,
    findInObj,
    isIsogram,
    matrixLetter,
    maxItems,
    moreCommonWord,
    oddAndEven,
    palindrome,
    scoreAverage,
} from '../src/utils';

test('canAllDrink', () => {
    expect(true).toBe(true);
});
